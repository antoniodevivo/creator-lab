{
  "name": "creator-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib/pipeline.mjs && node --check lib/providers.mjs && node --check public/app.js && node --check public/research.mjs && node --check public/record.js && node --check scripts/setup.mjs && node --check scripts/doctor.mjs",
    "setup": "node scripts/setup.mjs",
    "doctor": "node scripts/doctor.mjs"
  },
  "engines": {
    "node": ">=22.9.0"
  },
  "version": "1.0.0",
  "description": "Local Instagram Reel research with Apify, Jev, and Fireworks or Groq.",
  "license": "MIT"
}
